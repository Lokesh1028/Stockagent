import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Insider Trading Alerts - Stay Ahead of the Market",
  description: "Get real-time alerts on insider trading activity with intelligent analysis and actionable recommendations.",
  keywords: ["insider trading", "stock alerts", "investment", "financial analysis"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
